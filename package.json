{
  "name": "battlegamev2",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:three-vendor": "esbuild renderers/three.vendor.entry.js --bundle --minify --format=iife --platform=browser --target=es2020 --outfile=renderers/three.vendor.js",
    "build:single": "node scripts/build-single-file.mjs",
    "build": "npm run build:single"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  },
  "dependencies": {
    "three": "^0.183.0"
  }
}
